Ниже — **идеальная шпаргалка** по всем видам циклов Bash:  
**`for in`**, **`for ((…))`**, **`while`**, **`until`** — с примерами, разбором и когда какой лучше использовать.

---

# 🟩 **1) ЦИКЛ `for i in ...`**

Перебирает **список значений** (слова, строки, файлы, числа — что ты задашь).

## ✔ Пример 1: список чисел

```bash
for i in 1 2 3 4 5; do
  echo "$i"
done
```

## ✔ Пример 2: диапазон (brace expansion)

```bash
for i in {1..5}; do
  echo "$i"
done
```

## ✔ Пример 3: файлы в директории

```bash
for file in *.txt; do
  echo "$file"
done
```

## 🧠 Когда использовать?

|Когда|Почему|
|---|---|
|Если есть **готовый список значений**|Самый читаемый|
|Если перебираешь **файлы, пути, строки**|Это удобно|
|Если диапазон небольшой|Быстро и просто|

---

# 🟦 **2) ЦИКЛ `for ((…))` — C-style**

Используется для **арифметики**: счётчиков, шагов, сложной логики.

Синтаксис:

```bash
for ((init; condition; increment)); do
  commands
done
```

## ✔ Пример: от 0 до 4

```bash
for ((i=0; i<5; i++)); do
  echo "$i"
done
```

## ✔ Пример: шаг 2

```bash
for ((i=0; i<=10; i+=2)); do
  echo "$i"
done
```

## ✔ Пример: обратный отсчёт

```bash
for ((i=10; i>0; i--)); do
  echo "$i"
done
```

## 🧠 Когда использовать?

|Когда|Почему|
|---|---|
|Нужны **числовые циклы**|Самый быстрый способ|
|Нужен **шаг**, например i+=2|Очень удобно|
|Нужен **сложный инкремент**|Только этот цикл подходит|

---

# 🟨 **3) ЦИКЛ `while`**

Работает **пока условие истинно**.  
Полный контроль над логикой.

## ✔ Пример: от 1 до 5

```bash
COUNT=1
while [ $COUNT -le 5 ]; do
  echo "$COUNT"
  ((COUNT++))
done
```

## ✔ Пример: пока файл не исчезнет

```bash
while [ -e myfile.txt ]; do
  echo "Файл существует"
  sleep 1
done
```

## ✔ Пример: читать строки из файла

```bash
while read line; do
  echo ">> $line"
done < file.txt
```

## 🧠 Когда использовать?

|Когда|Почему|
|---|---|
|Условие может меняться в процессе|Максимальная гибкость|
|Цикл не обязательно числовой|Подходит для любых условий|
|Работа с потоками (`read`)|Только `while` подходит|

---

# 🟥 **4) ЦИКЛ `until`**

Противоположность `while`.

Работает **пока условие ЛОЖНО**.  
Цикл заканчивается, когда условие становится истинным.

## ✔ Пример: ждать, пока появится файл

```bash
until [ -e ready.flag ]; do
  echo "Ожидаю файл..."
  sleep 1
done
echo "Файл найден!"
```

## ✔ Пример: считать от 1 до 5, но наоборот

```bash
i=1
until [ $i -gt 5 ]; do
  echo "$i"
  ((i++))
done
```

## 🧠 Когда использовать?

|Когда|Почему|
|---|---|
|Нужно **выполнять до тех пор, пока условие не станет истинным**|Семантически удобнее|
|Проверка "пока чего-то НЕТ"|Читается проще, чем `while ! ...`|

---

# 🟩 Сводная таблица — когда какой цикл лучше

|Тип цикла|Лучшая область применения|
|---|---|
|**for i in …**|список значений, файлы, строки|
|**for ((…))**|числовые счётчики, шаги, математика|
|**while**|условия, потоки, сложные проверки|
|**until**|«выполнять, пока НЕ…»|

---

# 🟦 Хочешь шпаргалку в виде PDF?

Могу оформить красиво: таблица + примеры + синтаксис.