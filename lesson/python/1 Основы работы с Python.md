# Основы работы с Python

Основа по теме: что такое Python, как он выполняет код, переменные, синтаксис, комментарии/PEP 8 и первые функции `print()` / `input()`.

## 📖 Быстрая навигация по операторам и функциям

- [`=`](#4-оператор-присваивания) — оператор присваивания
- [`print()`](#10-print-вывод-на-экран) — вывод данных на экран
- [`input()`](#11-input-ввод-от-пользователя) — ввод данных от пользователя
- [`#`](#7-комментарии) — однострочный комментарий
- [`"""..."""`](#7-комментарии) — многострочный комментарий

**[📚 Дополнительная информация и примеры](#дополнительная-информация)**

---

## 1) Python: что это и почему его любят
**Python** — высокоуровневый язык программирования, известный простотой и читабельностью.

Ключевые особенности:
- **Простота** (код читается почти как английский текст)
- **Динамическая типизация** (тип определяется автоматически)
- **Интерпретируемость** (код выполняется “по строкам”)
- **Кроссплатформенность** (Windows/Linux/macOS)
- **Богатая стандартная библиотека**
- Поддержка **ООП** и **функционального подхода**
- Большое **сообщество** и экосистема библиотек

---

## 2) Интерпретатор vs компилятор (важная идея)
### Компилятор
- Сначала **переводит весь код** в машинный код
- Потом запускает
- Обычно быстрее на исполнении, но дольше подготовка

### Интерпретатор
- Выполняет код **построчно**, переводя “на лету”
- Удобно для обучения и отладки

✅ **Python — интерпретируемый язык**: запуск без предварительной компиляции.

---

## 3) Переменные и данные
### Что такое переменная
**Переменная** — именованная область памяти, где хранится значение, доступное по имени.

### Примеры данных
- `int` (целые): `5`, `-3`, `42`
- `float` (вещественные): `3.14`, `-0.001`, `2.0`
- `str` (строки): `"Привет"`, `'Мир'`

---

## 4) Оператор присваивания `=`
Оператор присваивания связывает переменную со значением.

```python
x = 10
name = "John"
```

⚠️ Не путать:
- `=` — присваивание
- `==` — сравнение (позже)

---

## 5) Правила именования переменных (чтобы не ловить ошибки)
Коротко и по делу:
- Имя может содержать **буквы, цифры, `_`**
- Начинается **только** с буквы или `_` (не с цифры)
- **Регистр важен**: `age` и `Age` — разные
- Нельзя использовать **ключевые слова** (`if`, `for`, `while`, …)
- Не называй переменные именами встроенных функций (`print`, `int`, `max`, …)
- По стилю: **snake_case** (`user_name`, `total_score`)
- Имя должно быть **понятным**, но не слишком длинным

✅ Хорошо:
```python
user_name = "Alice"
total_score = 100
_price = 10
```

❌ Плохо:
```python
2nd_place = 1       # начинается с цифры
my-variable = 10    # дефис нельзя
my variable = 10    # пробел нельзя
total%items = 5     # % нельзя
if = 42             # ключевое слово
```

---

## 6) Синтаксис Python: “закон отступов”
**Синтаксис** — правила написания кода, чтобы интерпретатор понял программу.

Главные правила:
- Обычно **1 строка = 1 инструкция**
- `;` можно, но **не рекомендуется**
- Блоки кода задаются **двоеточием `:`** и **отступами** (обычно 4 пробела)

Пример блока:
```python
a = 5
if a == 5:
    print("Hi")
    print("Hello")
```

### Ошибки синтаксиса (SyntaxError)
Частые причины:
- неправильные отступы
- забыли `:` после `if/for/while/def/...`
- ошибки в кавычках
- недопустимые символы в имени переменной

---

## 7) Комментарии
### Однострочный комментарий
```python
# Это комментарий
x = 5  # комментарий в конце строки
```

### Многострочный комментарий (или docstring-стиль)
```python
"""
Это многострочный комментарий/описание.
Может занимать несколько строк.
"""
```
(Также можно `''' ... '''`)

---

## 8) PEP 8 — базовые правила стиля
PEP 8 — рекомендации по оформлению кода, чтобы его было легче читать и поддерживать.

Минимум, который стоит соблюдать сразу:
- **Отступы:** 4 пробела
- **Длина строки:** до 79 символов
- **Имена:** `snake_case` для переменных/функций, `CamelCase` для классов, `UPPER_CASE` для констант
- Пробелы вокруг операторов: `x = 5`, `a + b`
- Импорты — в начале файла, группировать пустыми строками

---

## 9) Функции: что это и как вызвать
**Функция** — готовый блок кода, который выполняет задачу.

Чтобы вызвать функцию:
1) написать имя  
2) поставить `()`  
3) при необходимости — передать аргументы

```python
print("Привет, мир!")
```

---

## 10) `print()` — вывод на экран
```python
print("Hello, Python!")
print("Число:", 10)
print()  # выведет пустую строку
```

---

## 11) `input()` — ввод от пользователя
`input()` **всегда возвращает строку** (`str`).

```python
name = input("Введите ваше имя: ")
print("Привет, " + name + "!")
```

---

## 12) Мини-практика (как на занятии)
### 12.1 Приветствие
```python
print("Hello, Python!")
```

### 12.2 Ввод имени и вывод
```python
name = input("Введите ваше имя: ")
print(name)
```

### 12.3 Личное досье (имя + возраст + город)
```python
name = input("Введите имя: ")
age = input("Введите возраст: ")
city = input("Введите город: ")
print(f"{name} {age} лет, проживает в городе {city}")
```

### 12.4 Повторитель текста (3 раза)
```python
text = input("Введите текст: ")
print(text)
print(text)
print(text)
```

---

## 13) Быстрые ответы на задания (самопроверка)
- Оператор присваивания: **`=`**
- Корректные имена переменных из списка: **student_name, age_25, is_student, total_score**
- PEP 8 отступы: **4 пробела**
- Многострочный комментарий: **""" ... """** и **''' ... '''**
- `print(a b)` → **ошибка синтаксиса**
- “что делает вызов функции?” → **выполняет ранее написанную функцию**
- `print()` = вывод, `input()` = ввод

---

## 14) Шпаргалка “на пальцах”
```text
=  присваивание
== сравнение

if ...:    <-- двоеточие
    ...    <-- отступ 4 пробела

print(x)   <-- вывести
input()    <-- прочитать строку
```

---

## 📚 Дополнительная информация

### Важные концепции для изучения

#### 1. Типы данных и их преобразование
Хотя в уроке упоминаются базовые типы (`int`, `float`, `str`), важно понимать преобразование типов:

```python
# Преобразование строки в число
age = input("Введите возраст: ")  # Результат всегда строка
age_int = int(age)  # Преобразование в целое число
print(f"Через 5 лет вам будет {age_int + 5}")

# Преобразование числа в строку
number = 42
text = str(number)  # "42"
print("Число: " + text)  # Конкатенация строк

# Проверка типа
print(type(age))      # <class 'str'>
print(type(age_int))  # <class 'int'>
```

#### 2. Escape-последовательности в строках
```python
# Специальные символы
print("Первая строка\nВторая строка")  # \n - перенос строки
print("Имя:\tИван")                     # \t - табуляция
print("Путь: C:\\Users\\Documents")     # \\ - обратный слэш
print("Он сказал: \"Привет!\"")         # \" - кавычка в строке
```

#### 3. Множественное присваивание
```python
# Присваивание нескольких значений одновременно
x, y, z = 1, 2, 3
print(x, y, z)  # 1 2 3

# Одинаковое значение нескольким переменным
a = b = c = 0
print(a, b, c)  # 0 0 0

# Обмен значений (swap)
x, y = 5, 10
x, y = y, x  # Меняем местами
print(x, y)  # 10 5
```

#### 4. Базовые операции со строками
```python
# Конкатенация (склеивание)
first_name = "Иван"
last_name = "Петров"
full_name = first_name + " " + last_name
print(full_name)  # "Иван Петров"

# Повторение строк
print("=" * 20)  # ====================
print("Привет! " * 3)  # Привет! Привет! Привет!

# Длина строки
text = "Python"
print(len(text))  # 6
```

#### 5. Форматирование вывода
```python
# f-строки (современный способ)
name = "Мария"
age = 25
print(f"Меня зовут {name}, мне {age} лет")

# Метод format()
print("Меня зовут {}, мне {} лет".format(name, age))

# Старый стиль (%)
print("Меня зовут %s, мне %d лет" % (name, age))
```

### 💡 Практические примеры

#### Программа: Калькулятор возраста
```python
# Вычисление возраста через N лет
name = input("Введите ваше имя: ")
current_age = int(input("Введите ваш возраст: "))
years = int(input("Через сколько лет: "))

future_age = current_age + years
print(f"{name}, через {years} лет вам будет {future_age} лет")
```

#### Программа: Простой опрос
```python
# Создание мини-анкеты
print("=== АНКЕТА ===")
name = input("Имя: ")
city = input("Город: ")
hobby = input("Хобби: ")

print("\n--- ВАШИ ДАННЫЕ ---")
print(f"Имя: {name}")
print(f"Город: {city}")
print(f"Хобби: {hobby}")
print("=" * 20)
```

#### Программа: Генератор приветствий
```python
# Персонализированное приветствие
name = input("Как вас зовут? ")
time_of_day = input("Какое время суток (утро/день/вечер)? ")

# Формируем приветствие
if time_of_day == "утро":
    greeting = f"Доброе утро, {name}!"
elif time_of_day == "день":
    greeting = f"Добрый день, {name}!"
else:
    greeting = f"Добрый вечер, {name}!"

print(greeting)
print("Рады вас видеть в нашей программе!")
```

### 🚨 Частые ошибки начинающих

1. **Забывают преобразовать input() в число**
```python
# ❌ Неправильно
age = input("Возраст: ")
print(age + 5)  # TypeError: can only concatenate str

# ✅ Правильно
age = int(input("Возраст: "))
print(age + 5)
```

2. **Используют зарезервированные слова как имена**
```python
# ❌ Неправильно
print = 5  # Перезаписывает функцию print!

# ✅ Правильно
user_print = 5
```

3. **Смешивают одинарные и двойные кавычки**
```python
# ❌ Неправильно
text = 'Он сказал: "Привет!'  # SyntaxError

# ✅ Правильно
text = 'Он сказал: "Привет!"'
text = "Он сказал: \"Привет!\""
```

### 📌 Полезные ресурсы
- **PEP 8**: [python.org/dev/peps/pep-0008](https://www.python.org/dev/peps/pep-0008/)
- **Python документация**: [docs.python.org](https://docs.python.org/3/)
- **Встроенные функции**: `help()` в интерактивном режиме

---

