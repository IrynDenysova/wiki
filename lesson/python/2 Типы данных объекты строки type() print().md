# Типы данных объекты строки type() print()

## 📖 Быстрая навигация по операторам и функциям

- [[#1) Тип данных и объект]](#1-тип-данных-и-объект)
- [[#2) Динамическая типизация]](#2-динамическая-типизация)
- [[#3) Примитивные типы данных (базовые)]](#3-примитивные-типы-данных-базовые)
- [[#4) Возвращаемое значение (return value)]](#4-возвращаемое-значение-return-value)
- [[#5) “Процедуры” в Python (термин из практики)]](#5-“процедуры”-в-python-термин-из-практики)
- [[#6) Передача аргументов в функцию (4 способа)]](#6-передача-аргументов-в-функцию-4-способа)
- [[#7) `type()` — узнать тип объекта]](#7-type-—-узнать-тип-объекта)
- [[#8) Строковые операции]](#8-строковые-операции)
- [[#9) Кавычки в строках: как выбрать правильно]](#9-кавычки-в-строках-как-выбрать-правильно)
- [[#10) Экранирование символов `\`]](#10-экранирование-символов-\)
- [[#11) `print()` параметры `sep` и `end`]](#11-print-параметры-sep-и-end)
- [[#12) Быстрые ответы на задания из урока (самопроверка)]](#12-быстрые-ответы-на-задания-из-урока-самопроверка)
- [[#13) Практическая работа (готовые решения)]](#13-практическая-работа-готовые-решения)
- [[#14) Домашнее задание (шаблоны решений)]](#14-домашнее-задание-шаблоны-решений)
- [[#15) Мини-шпаргалка (самое важное)]](#15-мини-шпаргалка-самое-важное)
- [[#Дополнительная информация]](#дополнительная-информация)

**[[#📚 Дополнительная информация]](#дополнительная-информация)**

---

Материалы урока: **"Базовые типы данных, работа с функциями"**.  
(Конспект сделан так, чтобы по нему можно было быстро повторить тему и решать ДЗ.)

---

## 1) Тип данных и объект
**Тип данных** определяет:
- какие значения может хранить объект
- какие операции можно выполнять с этими значениями

**Объект** в Python — это **любое значение**, с которым можно работать в программе.  
В Python “всё — объект”: числа, строки, списки и т.д.

💡 Важно: тип присваивается **не переменной**, а **объекту**, на который переменная “ссылается”.

---

## 2) Динамическая типизация
Python — **динамически типизированный** язык:
- тип переменной определяется автоматически в момент присваивания
- переменная может начать ссылаться на объект другого типа

```python
price = 10      # int
price = 9.99    # float (тип изменился автоматически)
```

---

## 3) Примитивные типы данных (базовые)
### `int` — целые числа
```python
x = 5
y = -3
z = 0
```

### `float` — числа с плавающей точкой
```python
pi = 3.14
temperature = -5.2
```

### `str` — строки
Строки можно писать:
- в одинарных `'...'`
- в двойных `"..."`

А также в тройных кавычках для многострочного текста:
- `'''...'''` или `"""..."""`

```python
single_quote_string = 'Это строка в одинарных кавычках.'
double_quote_string = "Это строка в двойных кавычках."

triple_quote_string = '''Это строка в
тройных кавычках, которая
может занимать несколько строк.'''
```

### `bool` — логические значения
Только два значения:
- `True`
- `False`

```python
is_student = True
has_passed = False
```

### `NoneType` — отсутствие значения
Единственное значение: `None`.

Зачем используют:
- “значение пока неизвестно”
- “функция ничего не возвращает” (по умолчанию возвращается `None`)

```python
value = None
```

---

## 4) Возвращаемое значение (return value)
**Возвращаемое значение** — результат, который функция передаёт обратно в место вызова.

Функция может:
- вернуть значение
- не вернуть ничего → вернётся `None`

---

## 5) “Процедуры” в Python (термин из практики)
В Python формально нет отдельного типа “процедура”, но так часто называют функции, которые:
- выполняют действие,
- и ничего не возвращают (или возвращают `None`).

Классический пример — `print()`:
```python
value = print("Привет, мир!")  # печатает текст
print(value)                   # выведет: None
```

---

## 6) Передача аргументов в функцию (4 способа)
Можно передавать:
1) **Литералы** (значения напрямую)
```python
print(5)
print("Привет, мир!")
```

2) **Переменные**
```python
text = "Привет, мир!"
print(text)
```

3) **Результаты выражений**
```python
print(2 + 5)
```

4) **Результат другой функции**
```python
print(input("Введите имя: "))
```

---

## 7) `type()` — узнать тип объекта
`type(x)` возвращает тип (класс) объекта.

```python
x = 10
print(type(x))  # <class 'int'>

x = "Привет"
print(type(x))  # <class 'str'>
```

---

## 8) Строковые операции
### 8.1 Конкатенация (склейка строк): `+`
```python
str1 = "Привет"
str2 = "мир"
result = str1 + " " + str2
print(result)  # Привет мир
```

### 8.2 Умножение строк: `*`
```python
str1 = "Hi! "
result = str1 * 3
print(result)  # Hi! Hi! Hi!
```

---

## 9) Кавычки в строках: как выбрать правильно
Рекомендация:
- если внутри строки нужны **одинарные кавычки**, снаружи ставь **двойные**
- если внутри нужны **двойные**, снаружи ставь **одинарные**
Так можно избежать экранирования.

```python
quote1 = "Она сказала: 'Привет!'"
quote2 = 'Он ответил: "Привет!"'
```

Если и те, и другие кавычки внутри — удобно использовать **тройные кавычки**:
```python
multi_line_string = """Тут есть 'одинарные' и "двойные" кавычки."""
```

---

## 10) Экранирование символов `\`
Экранирование — способ “вставить” спецсимволы в строку:

- `\n` — новая строка  
- `\t` — табуляция  
- `\'` — одинарная кавычка внутри `'...'`
- `\"` — двойная кавычка внутри `"..."`

Примеры:
```python
string1 = 'В строке есть \'одинарные\' и "двойные" кавычки.'
string2 = "В строке есть 'одинарные' и \"двойные\" кавычки."

text = "Первая строка\nВторая строка"
text_with_tab = "Первая строка\tВторая строка"
```

Если нужен обратный слэш в пути Windows — его экранируют:
```python
path = "C:\\Users\\Username\\Documents"
```

---

## 11) `print()` параметры `sep` и `end`
### `sep` — разделитель между аргументами
По умолчанию пробел.

```python
print("one", "two", "three")              # one two three
print("one", "two", "three", sep="---")   # one---two---three
```

### `end` — что добавить в конец вывода
По умолчанию перевод строки `\n`.

```python
print("Привет,", "мир", end="!!")
print("Привет")  # эта строка продолжит вывод после "!!"
```

---

## 12) Быстрые ответы на задания из урока (самопроверка)
- `x = 5.0` → тип: **float**
- Логическое значение из вариантов `"True"`, `0`, `True`, `None` → **True**
- Если сначала `int`, потом `str` → тип **изменится автоматически**
- Если передать переменную в `print()` → будет выведено **значение переменной**
- `str1="Привет"; str2="мир"; str1 + str2` → **"Приветмир"**
- `"Hi! " * 3` → **"Hi! Hi! Hi!"**

---

## 13) Практическая работа (готовые решения)
### 13.1 “Приветствие с восклицанием” через `end`
```python
print("Привет, мир", end="!")
```

### 13.2 “Цитата” (2 способа)
Нужно вывести: `She said: "It's amazing!"`

**Способ 1 (выбрать правильные кавычки):**
```python
print('She said: "It\'s amazing!"')
```

**Способ 2 (экранирование двойных кавычек):**
```python
print("She said: \"It's amazing!\"")
```

---

## 14) Домашнее задание (шаблоны решений)
### 14.1 “Числовая цепочка” (sep)
Вывести `1---2---3---4---5`
```python
print(1, 2, 3, 4, 5, sep="---")
```

### 14.2 “Пробельные символы” (\t и \n) — одним `print()`
```python
print("Первая строка с табуляцией\n\tВторая строка на новой строке")
```

### 14.3 “Текст в кавычках”
Вывести: `Это файл "example.txt"`
```python
print('Это файл "example.txt"')
# или
print("Это файл \"example.txt\"")
```

---

## 15) Мини-шпаргалка (самое важное)
```text
Тип у объекта, а не у переменной → динамическая типизация
int, float, str, bool, None

type(x) → узнать тип
'...' / "..." / '''...''' / """..."""

+  для строк → конкатенация
*  для строк → повторение

\n новая строка
\t табуляция
\\ обратный слэш

print(*args, sep="...", end="...")
```

---

## Дополнительная информация

### Важные концепции для изучения

#### 1. Изменяемость и неизменяемость типов данных
Строки в Python **неизменяемы** (immutable):
```python
text = "привет"
# text[0] = "П"  # ОШИБКА! Нельзя изменить символ
text = "Привет"  # OK: создается новый объект
```

**Изменяемые типы** (mutable): `list`, `dict`, `set`  
**Неизменяемые типы** (immutable): `int`, `float`, `str`, `tuple`, `bool`

#### 2. Проверка принадлежности подстроки
Оператор `in` проверяет, содержится ли подстрока:
```python
text = "Python программирование"
print("Python" in text)     # True
print("Java" in text)       # False
print("python" in text)     # False (регистрозависимо!)
```

#### 3. Сырые строки (raw strings)
Для путей Windows удобно использовать `r"..."`:
```python
# Обычная строка (нужно экранирование)
path1 = "C:\\Users\\Username\\Documents"

# Сырая строка (автоматически без экранирования)
path2 = r"C:\Users\Username\Documents"

print(path1 == path2)  # True
```

#### 4. Форматирование с f-строками
Современный способ вставки переменных:
```python
name = "Алиса"
age = 25
print(f"Меня зовут {name}, мне {age} лет")
# Меня зовут Алиса, мне 25 лет

# Выражения внутри {}
price = 99.99
print(f"Цена со скидкой: {price * 0.9:.2f} руб.")
# Цена со скидкой: 89.99 руб.
```

#### 5. Методы строк (основные)
```python
text = "  Python Programming  "
print(text.strip())      # "Python Programming" (убрать пробелы)
print(text.lower())      # "  python programming  "
print(text.upper())      # "  PYTHON PROGRAMMING  "
print(text.replace("Python", "Java"))  # "  Java Programming  "
print(text.split())      # ['Python', 'Programming']
```

### 💡 Практические примеры

#### Пример 1: Визитная карточка
```python
name = input("Введите имя: ")
age = input("Введите возраст: ")
city = input("Введите город: ")

print("=" * 30)
print(f"Имя:\t\t{name}")
print(f"Возраст:\t{age}")
print(f"Город:\t\t{city}")
print("=" * 30)

# Пример вывода:
# ==============================
# Имя:		Иван
# Возраст:	28
# Город:	Москва
# ==============================
```

#### Пример 2: Генератор ASCII-рамки
```python
text = "PYTHON"
width = len(text) + 4

print("+" + "-" * (width - 2) + "+")
print("|" + " " * (width - 2) + "|")
print(f"| {text} |")
print("|" + " " * (width - 2) + "|")
print("+" + "-" * (width - 2) + "+")

# Вывод:
# +----------+
# |          |
# | PYTHON |
# |          |
# +----------+
```

#### Пример 3: Проверка типов данных
```python
values = [42, 3.14, "текст", True, None]

for val in values:
    print(f"Значение: {val:<10} Тип: {type(val).__name__}")

# Вывод:
# Значение: 42         Тип: int
# Значение: 3.14       Тип: float
# Значение: текст      Тип: str
# Значение: True       Тип: bool
# Значение: None       Тип: NoneType
```

### 🚨 Частые ошибки начинающих

**Ошибка 1: Смешивание типов без преобразования**
```python
# ❌ НЕПРАВИЛЬНО
age = 25
# print("Возраст: " + age)  # TypeError!

# ✅ ПРАВИЛЬНО
print("Возраст: " + str(age))
print(f"Возраст: {age}")
```

**Ошибка 2: Забыть про экранирование**
```python
# ❌ НЕПРАВИЛЬНО
# path = "C:\new\text.txt"  # \n и \t интерпретируются!

# ✅ ПРАВИЛЬНО
path = "C:\\new\\text.txt"
path = r"C:\new\text.txt"
```

**Ошибка 3: Путаница с кавычками**
```python
# ❌ НЕПРАВИЛЬНО
# text = "She said: "Hello""  # Синтаксическая ошибка!

# ✅ ПРАВИЛЬНО
text = 'She said: "Hello"'
text = "She said: \"Hello\""
```

### 📌 Полезные ресурсы
- [Документация Python: Встроенные типы](https://docs.python.org/3/library/stdtypes.html)
- [Документация: Строковые методы](https://docs.python.org/3/library/stdtypes.html#string-methods)
- [PEP 498: f-строки](https://peps.python.org/pep-0498/)
