# Арифметические операторы приоритет типы

## 📖 Быстрая навигация по операторам и функциям

- [[#1) Числа в Python]](#1-числа-в-python)
- [[#2) Арифметические операции]](#2-арифметические-операции)
- [[#3) Переменные и “одноразовые” вычисления]](#3-переменные-и-“одноразовые”-вычисления)
- [[#4) Ошибка `ZeroDivisionError`]](#4-ошибка-zerodivisionerror)
- [[#5) Приоритет операций (как в математике)]](#5-приоритет-операций-как-в-математике)
- [[#6) Неизменяемые (immutable) типы данных — идея]](#6-неизменяемые-immutable-типы-данных-—-идея)
- [[#7) Операторы приращения (сокращённое присваивание)]](#7-операторы-приращения-сокращённое-присваивание)
- [[#8) Множественное присваивание]](#8-множественное-присваивание)
- [[#9) Преобразование типов (Type Casting)]](#9-преобразование-типов-type-casting)
- [[#10) Ошибка `ValueError`]](#10-ошибка-valueerror)
- [[#11) Практическая работа (из урока) — решения]](#11-практическая-работа-из-урока-—-решения)
- [[#12) Домашнее задание — подсказки решений]](#12-домашнее-задание-—-подсказки-решений)
- [[#13) Мини-шпаргалка]](#13-мини-шпаргалка)
- [[#Дополнительная информация]](#дополнительная-информация)

**[[#Дополнительная информация]](#дополнительная-информация)**

---

## 1) Числа в Python
Базовые числовые типы:
- `int` — целые числа
- `float` — числа с плавающей точкой (вещественные)

---

## 2) Арифметические операции
| Оператор | Название | Что делает |
|---|---|---|
| `+` | сложение | складывает значения |
| `-` | вычитание | вычитает одно из другого |
| `*` | умножение | умножает значения |
| `/` | деление | **всегда** даёт `float` результат |
| `//` | целочисленное деление | берёт целую часть, **округляет вниз** |
| `%` | остаток от деления | возвращает остаток |
| `**` | степень | возводит в степень | fileciteturn36file0 fileciteturn36file1

Важно:
- при обычном делении `/` результат всегда дробный (`float`);
- если хотя бы один операнд `float`, результат обычно `float`;
- `//` округляет **в меньшую сторону** (floor). 

### Примеры
```py
print(3 + 5)     # 8
print(7 - 2)     # 5
print(4 * 6.0)   # 24.0
print(8 / 2)     # 4.0
print(7 // 2)    # 3
print(7 % 3)     # 1
print(2 ** 3)    # 8
```


---

## 3) Переменные и “одноразовые” вычисления
- Если результат понадобится повторно — сохраняем в переменную.
- Если действие делается один раз — хранить в переменной не обязательно. 

```py
sum1 = 3 + 5
a = sum1 + 1
b = sum1 * 3
print(a, b)
```

---

## 4) Ошибка `ZeroDivisionError`
`ZeroDivisionError` возникает при:
- делении на ноль (`/` и `//`);
- остатке от деления на ноль (`%`).

```py
a = 10
b = 0
print(a / b)   # ZeroDivisionError
```

---

## 5) Приоритет операций (как в математике)
Порядок выполнения (от высокого к низкому):
1. `()` скобки
2. `**` степень
3. `*`, `/`, `//`, `%` (слева направо)
4. `+`, `-` (слева направо) 

Примеры:
```py
print((2 + 3) * 4)   # 20
print(2 * 3 ** 2)    # 18, т.к. 2 * (3 ** 2)
print(10 / 2 * 3)    # 15.0, слева направо: (10/2)*3
print(10 - 2 + 5)    # 13, слева направо: (10-2)+5
```

---

## 6) Неизменяемые (immutable) типы данных — идея
В Python есть:
- **неизменяемые** типы (immutable) — нельзя “поменять объект”, можно только создать новый и перепривязать переменную;
- **изменяемые** (mutable) — можно менять содержимое объекта. 

Пример идеи (immutable на числе):
```py
num = 5
num + 2
print(num)      # 5 (ничего не изменилось)

num = num + 2
print(num)      # 7 (переприсвоили)
```

---

## 7) Операторы приращения (сокращённое присваивание)
| Оператор | Эквивалент |
|---|---|
| `a += 1` | `a = a + 1` |
| `a -= 1` | `a = a - 1` |
| `a *= 2` | `a = a * 2` |
| `a /= 2` | `a = a / 2` |
| `a //= 2` | `a = a // 2` |
| `a %= 3` | `a = a % 3` |
| `a **= 2` | `a = a ** 2` | fileciteturn36file0 fileciteturn36file1

Примеры:
```py
a = 5
a += 1
print(a)  # 6

a = 5
a *= 2
print(a)  # 10
```

---

## 8) Множественное присваивание
### Несколько значений за раз
```py
a, b, c = 1, 2, 3
print(a, b, c)  # 1 2 3
```

### Одно значение — нескольким переменным
```py
a = b = c = 0
print(a, b, c)  # 0 0 0
```

### Обмен значениями без временной переменной
```py
a, b = 5, 10
a, b = b, a
print(a, b)  # 10 5
```

---

## 9) Преобразование типов (Type Casting)
Преобразование типов нужно, когда данные разных типов нельзя напрямую использовать в операции  
(например, деление строк вызовет `TypeError`).

### Неявное преобразование (implicit)
Python иногда сам приводит типы, например `int -> float`:
```py
result = 5
result += 3.14
print(type(result))  # float
```

### Явное преобразование (explicit)
Встроенные функции:
- `int("10")`
- `float("10.5")`
- `str(10)`
- `bool(1)` 

Примеры:
```py
a = 5
b = str(a)     # "5"

a = "10"
b = int(a)     # 10

a = 10.7
b = int(a)     # 10 (дробная часть отбрасывается)
```

---

## 10) Ошибка `ValueError`
`ValueError` возникает, когда тип аргумента допустим, но **значение** некорректно (например, строка не число).

```py
a = "abc"
b = float(a)   # ValueError
```

---

## 11) Практическая работа (из урока) — решения

### 11.1 Определите математические операции (a=7, b=3 → получить 4, 1, 2)
```py
a = 7
b = 3
print("Результат операции 'Разность':", a - b)              # 4
print("Результат операции 'Остаток от деления':", a % b)     # 1
print("Результат операции 'Целочисленное деление':", a // b) # 2
```

### 11.2 Расчёт сдачи
```py
price = int(input("Введите стоимость товара: "))
quantity = int(input("Введите количество товара: "))
payment = int(input("Введите сумму оплаты: "))

total_cost = price * quantity
change = payment - total_cost

print("Сдача:", change, "рублей")
```

---

## 12) Домашнее задание — подсказки решений

### 12.1 Сумма цифр 4-значного числа
Идея: взять строку и сложить `int()` по символам.
```py
n = input("Введите четырехзначное число: ")
s = int(n[0]) + int(n[1]) + int(n[2]) + int(n[3])
print("Сумма цифр числа:", s)
```

### 12.2 Умножение чисел (не сохранять произведение в переменную) и вывести через дефис
```py
a = int(input("Введите первое число: "))
b = int(input("Введите второе число: "))
print("Результат:", a * b, "-", a, "-", b)
```

### 12.3 Остаток и целочисленное деление без `%` и `//`
Идея:
- целая часть: `q = int(a / b)` (для положительных чисел как в примерах)
- остаток: `r = a - q*b`
```py
a = int(input("Введите первое число: "))
b = int(input("Введите второе число: "))

q = int(a / b)
r = a - q * b

print("Остаток от деления:", r)
print("Целочисленное деление:", q)
```

---

## 13) Мини-шпаргалка
```text
/  -> float
// -> floor (вниз)
%  -> остаток
** -> степень

Приоритет:
() -> ** -> * / // % -> + -

+= -= *= /= //= %= **=

int("10"), float("3.14"), str(10), bool(0)

ZeroDivisionError: деление/остаток на 0
ValueError: нельзя преобразовать значение (например "abc" -> int/float)
```

---

## Дополнительная информация

### Важные концепции для изучения

#### 1. Особенности деления в Python
```python
# Обычное деление ВСЕГДА возвращает float
print(10 / 5)      # 2.0 (не 2!)
print(10 / 3)      # 3.3333333333333335

# Целочисленное деление округляет ВНИЗ (floor)
print(10 // 3)     # 3
print(-10 // 3)    # -4 (не -3! округление вниз)

# Остаток от деления имеет знак делителя
print(10 % 3)      # 1
print(-10 % 3)     # 2 (не -1!)
print(10 % -3)     # -2
```

#### 2. Точность вычислений с float
```python
# Проблема точности float
print(0.1 + 0.2)   # 0.30000000000000004 (не 0.3!)

# Решение: округление
result = round(0.1 + 0.2, 2)  # 0.3

# Или модуль decimal для точных вычислений
from decimal import Decimal
x = Decimal('0.1') + Decimal('0.2')
print(x)  # 0.3
```

#### 3. Комбинированные операции
```python
# Порядок имеет значение!
x = 10
x = x + 5 * 2  # x = 10 + 10 = 20
print(x)

x = 10
x *= 2 + 3     # x = 10 * 5 = 50 (не 23!)
print(x)

# Инкремент и декремент (НЕТ ++ и --)
x = 5
x += 1  # x = 6 (нет x++ в Python!)
x -= 1  # x = 5 (нет x-- в Python!)
```

#### 4. Возведение в степень и корни
```python
# Степень
print(2 ** 3)      # 8
print(2 ** 0.5)    # 1.4142135623730951 (квадратный корень)

# Корень n-й степени
print(27 ** (1/3)) # 3.0 (кубический корень)
print(16 ** 0.25)  # 2.0 (корень 4-й степени)

# Модуль pow() с третьим аргументом (по модулю)
print(pow(2, 3))      # 8
print(pow(2, 3, 5))   # 3 (остаток от 8 / 5)
```

#### 5. Работа с большими числами
```python
# Python автоматически работает с очень большими числами
big = 10 ** 100
print(big)  # 100 нулей!

# Научная нотация
x = 1.5e3   # 1500.0
y = 2e-4    # 0.0002
print(x, y)
```

### 💡 Практические примеры

#### Пример 1: Конвертер температур
```python
celsius = float(input("Температура в Цельсиях: "))

fahrenheit = celsius * 9/5 + 32
kelvin = celsius + 273.15

print(f"{celsius}°C = {fahrenheit:.1f}°F")
print(f"{celsius}°C = {kelvin:.2f}K")

# Пример: 25°C = 77.0°F и 298.15K
```

#### Пример 2: Калькулятор времени
```python
total_seconds = int(input("Введите количество секунд: "))

hours = total_seconds // 3600
minutes = (total_seconds % 3600) // 60
seconds = total_seconds % 60

print(f"{total_seconds} секунд = {hours}ч {minutes}м {seconds}с")

# Пример: 3665 секунд = 1ч 1м 5с
```

#### Пример 3: Разделение суммы на купюры
```python
amount = int(input("Сумма для обмена: "))

bills_1000 = amount // 1000
amount %= 1000

bills_500 = amount // 500
amount %= 500

bills_100 = amount // 100
amount %= 100

print(f"1000 руб: {bills_1000} шт")
print(f"500 руб: {bills_500} шт")
print(f"100 руб: {bills_100} шт")
print(f"Остаток: {amount} руб")
```

### 🚨 Частые ошибки начинающих

**Ошибка 1: Путаница / и //**
```python
# ❌ НЕПРАВИЛЬНО
# age_in_years = total_days / 365  # Дробное число!

# ✅ ПРАВИЛЬНО
age_in_years = total_days // 365  # Целое количество лет
```

**Ошибка 2: Забыть про приоритет операций**
```python
# ❌ НЕПРАВИЛЬНО
result = 10 + 5 * 2  # 20, а не 30!

# ✅ ПРАВИЛЬНО
result = (10 + 5) * 2  # 30
```

**Ошибка 3: Деление на ноль**
```python
# ❌ НЕПРАВИЛЬНО
# x = 10 / 0  # ZeroDivisionError!

# ✅ ПРАВИЛЬНО
divisor = int(input("Делитель: "))
if divisor != 0:
    result = 10 / divisor
    print(result)
else:
    print("Ошибка: деление на ноль!")
```

### 📌 Полезные ресурсы
- [Документация Python: Числовые типы](https://docs.python.org/3/library/stdtypes.html#numeric-types-int-float-complex)
- [Документация: Операторы](https://docs.python.org/3/reference/expressions.html#binary-arithmetic-operations)
- [PEP 238: Изменение деления](https://peps.python.org/pep-0238/)
