1. Скрипт, который делает коммиты и пушит через SSH.
    
2. Настройку CRON, чтобы он запускался **раз в минуту / раз в час / раз в день** (ты выбираешь).
    
3. Правильный лог-файл, чтобы видеть, что всё работает.
    

---

# ✅ **1. Скрипт для автокоммитов и пуша (SSH версия)**

Сохрани его например как:

```
/opt/git_autopush.sh
```

---

## 🟩 **/opt/git_autopush.sh**

```bash
#!/bin/bash

# Параметры
GITHUBREPO=git@github.com:aliaskov/testrepo.git
REPONAME=/tmp/testrepo
LOGFILE=/var/log/git_autopush.log

# Создаем директорию для репозитория
mkdir -p $REPONAME
cd $REPONAME

# Если репозиторий еще не инициализирован — создаём
if [ ! -d .git ]; then
    echo "$(date) — initializing git repo" >> $LOGFILE
    git init
    git remote add origin $GITHUBREPO
    git config user.name "AutoCommitBot"
    git config user.email "bot@example.com"
fi

# Добавляем строку с текущей датой
date >> README.md

# Коммит
git add README.md
git commit -m "Auto commit: $(date '+%Y-%m-%d %H:%M:%S')" >> $LOGFILE 2>&1

# Пушим
git push >> $LOGFILE 2>&1
```

---

# 📌 Пояснение:

### ✔ Скрипт создаёт или обновляет README.md

### ✔ Коммит подписан текущей датой

### ✔ Все действия логируются в `/var/log/git_autopush.log`

### ✔ Push через SSH (без пароля)

---

# ✅ **2. Дать права на исполнение**

```bash
sudo chmod +x /opt/git_autopush.sh
```

---

# ✅ **3. Добавить скрипт в CRON**

Открываем cron:

```bash
crontab -e
```

---

# 🔥 Вариант A — запуск раз в минуту

```cron
* * * * * /opt/git_autopush.sh
```

---

# 🔥 Вариант B — запуск каждые 10 минут

```cron
*/10 * * * * /opt/git_autopush.sh
```

---

# 🔥 Вариант C — раз в час

```cron
0 * * * * /opt/git_autopush.sh
```

---

# 🔥 Вариант D — раз в день в 01:00

```cron
0 1 * * * /opt/git_autopush.sh
```

---

# 👀 Проверить логи

```bash
cat /var/log/git_autopush.log
```

---

# ☑ Проверить, что CRON действительно запускается

```bash
grep CRON /var/log/syslog
```

или в Amazon Linux:

```bash
sudo cat /var/log/cron
```

---

# 🎯 Готово!

Теперь коммиты будут появляться в GitHub автоматически по расписанию.

---

# ❓ Хочешь добавить:

- автосоздание веток?
    
- пуш в несколько репозиториев?
    
- защиту от конфликтов, если repo изменили вручную?
    
- контейнеризацию (Docker)?
    

Скажи — сделаю.