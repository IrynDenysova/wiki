
```bash
#!/bin/bash
#TES_DIR="/opt/101025-ptm/VAndreev"
#TES_DIR=$1
#echo "Enter dir name: "
read -p "Enter dir name: "  TES_DIR
for FILE in $TES_DIR/*
do
   if [ -d $FILE ];
     then
       echo "$FILE - It's directory"
     else
       echo "$FILE - It's file"
   fi
done
```

```bash
#!/bin/bash
#
DIR=/opt/101025-ptm/VAndreev
DATE=`date '+%Y-%m-%d'`
echo $DATE
#for i in {1..10}
#   do
#    date '+%H-%M-%S' > $DIR/File-$i.txt
#    sleep 2
#   done
#
count=1
while ((count <=10))
  do
    date '+%H-%M-%S' > $DIR/File-$count.txt
    ls -al $DIR/File-$count.txt
    sleep 1
    ((count++))
  done

mkdir -p $DIR/$DATE
cp $DIR/File*.txt $DIR/$DATE/
for j in $DIR/$DATE/*
  do
    cat $j
  done
```

