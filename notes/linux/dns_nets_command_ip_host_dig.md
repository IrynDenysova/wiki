
---

# 🧭 **Шпаргалка по DNS и сетевым командам (Debian / Linux)**

---

## ✅ Заглавие

|Категория|Основные команды|
|---|---|
|Проверка сети|`ip a`, `ping`, `traceroute`|
|DNS-резолвинг|`dig`, `host`, `nslookup`|
|Почтовые записи|`dig MX/TXT`, `host -t MX/TXT`|
|Проверка портов|`ss`, `netstat`, `nmap`, `lsof`|
|Whois-инфо|`whois`|
|Проверка маршрута|`mtr`, `traceroute`|

## ⚙️ Установка пакетов (один раз)

Чтобы иметь все нужные утилиты:

```bash
sudo apt update
sudo apt install -y dnsutils inetutils-traceroute net-tools iproute2 nmap whois
```

---

## 🌐 1. Проверка сети и IP-интерфейсов

|Команда|Назначение|Пример / пояснение|
|---|---|---|
|`ip a`|Показать все сетевые интерфейсы и IP|Проверяешь `eth0`, `lo`, IPv4/IPv6|
|`ip route`|Таблица маршрутизации|Показывает шлюз и маршрут по умолчанию|
|`hostname -I`|Текущий IP (коротко)|Только IP-адреса, без подробностей|
|`ping 8.8.8.8`|Проверка связи с интернетом|ICMP-пакеты к Google DNS|
|`ping google.com`|Проверка DNS-резолвинга|Если IP пингуется, а имя — нет → проблема DNS|
|`traceroute google.com`|Путь пакетов до цели|Помогает видеть задержки/блокировки|
|`netstat -tulnp`|Просмотр открытых портов и процессов|показывает, кто слушает 22/80/443 и т.д.|
|`ss -tulnp`|Современная альтернатива netstat|аналогично, но быстрее|

---

## 🧩 2. Проверка DNS-записей

### 🔍 Основная команда: `dig`

> Утилита для просмотра DNS (входит в `dnsutils`)

|Пример|Что делает|
|---|---|
|`dig s1.viktorplus.com`|Полный ответ DNS-сервера|
|`dig s1.viktorplus.com +short`|Только IP-адрес|
|`dig -x 185.182.184.40 +short`|Проверка PTR (обратная запись)|
|`dig MX viktorplus.com +short`|Почтовые серверы (MX)|
|`dig NS viktorplus.com +short`|Серверы имён (NS)|
|`dig TXT viktorplus.com +short`|Проверка SPF, DKIM, DMARC|
|`dig @8.8.8.8 s1.viktorplus.com`|Запрос к конкретному DNS-серверу|

---

### 🔎 Быстрая альтернатива: `host`

> Более компактная утилита (входит в тот же `dnsutils`)

|Команда|Результат|
|---|---|
|`host s1.viktorplus.com`|Покажет IP-адрес|
|`host 185.182.184.40`|Проверит PTR-запись|
|`host -t MX viktorplus.com`|Почтовые записи|
|`host -t TXT viktorplus.com`|SPF, DKIM, DMARC|

---

## ✉️ 3. Проверка почтовых записей домена

|Тип|Назначение|Пример проверки|
|---|---|---|
|**A**|Указывает IP сервера|`dig s1.viktorplus.com +short`|
|**MX**|Указывает почтовые серверы|`dig MX viktorplus.com +short`|
|**TXT**|Текстовые данные (SPF, DKIM, DMARC)|`dig TXT viktorplus.com +short`|
|**PTR**|Обратное соответствие IP → имя|`dig -x 185.182.184.40 +short`|
|**CNAME**|Псевдоним для другого домена|`dig CNAME mail.viktorplus.com +short`|

---

## 🔒 4. Проверка портов и сервисов

|Команда|Назначение|
|---|---|
|`sudo ss -tulnp`|Список прослушиваемых портов и PID процессов|
|`sudo netstat -plnt`|Аналогичный список (старый стиль)|
|`sudo lsof -i -P -n|grep LISTEN`|
|`sudo nmap -sS localhost`|Сканирование открытых TCP-портов (локально)|
|`sudo nmap -Pn s1.viktorplus.com`|Сканирование сервера снаружи|

💡 Примеры:

- порт `22` → SSH
    
- порт `80` → HTTP
    
- порт `443` → HTTPS
    
- порт `25`, `465`, `587` → SMTP
    
- порт `993` → IMAPS
    

---

## 🧠 5. Проверка маршрутов и соединений

|Команда|Назначение|
|---|---|
|`curl -I https://viktorplus.com`|Проверка заголовков сайта (200 OK и т.д.)|
|`wget -O- https://viktorplus.com`|Скачать и показать HTML|
|`telnet viktorplus.com 25`|Проверка SMTP-порта вручную|
|`nc -zv s1.viktorplus.com 25`|Проверка, открыт ли порт|
|`mtr 8.8.8.8`|Совмещённый ping + traceroute (если установлен `mtr`)|

---

## 🧱 6. Whois и информация о домене/IP

|Команда|Назначение|
|---|---|
|`whois viktorplus.com`|Регистратор, NS, даты регистрации|
|`whois 185.182.184.40`|Провайдер, диапазон IP|
|`dig SOA viktorplus.com +short`|Админская информация (Start Of Authority)|

---

## 🧰 7. Быстрые примеры для DNS-диагностики

### 🔸 Проверить всё подряд:

```bash
dig viktorplus.com A MX TXT NS +noall +answer
```

### 🔸 Проверить почтовые записи:

```bash
dig MX viktorplus.com +short
dig TXT viktorplus.com +short
```

### 🔸 Проверить совпадение A и PTR:

```bash
dig s1.viktorplus.com +short
dig -x 185.182.184.40 +short
```

---

## ⚡️ Полезный приём: использовать Google или Cloudflare DNS напрямую

```bash
dig @8.8.8.8 s1.viktorplus.com +short      # Google DNS
dig @1.1.1.1 s1.viktorplus.com +short      # Cloudflare DNS
```

Это помогает, если твой провайдер кэширует старые данные.

---