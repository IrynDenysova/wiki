# **Общий план обучения и настройки**

|Этап|Задача|Цель / навык|Примерные команды и примечания|
|---|---|---|---|
|**1. Первичная настройка системы**|🔹 Обновление, пользователи, SSH, безопасность|Научиться безопасно работать с сервером|`bash apt update && apt -y upgrade apt install -y sudo vim ufw fail2ban htop net-tools curl wget`  <br>Создать пользователя: `adduser viktor && usermod -aG sudo viktor`  <br>Ключевой доступ: `~/.ssh/authorized_keys`  <br>Отключить root-логин по паролю|
|**2. Сеть и DNS**|🔹 Установить hostname, проверить IP, PTR, DNS|Понимание сетевых основ|`bash hostnamectl set-hostname srv1.example.com`  <br>`ip a`, `ping 8.8.8.8`, `dig example.com`|
|**3. UFW и Fail2Ban**|🔹 Ограничить доступ, разрешить нужные порты|Безопасность|`bash ufw default deny incoming; ufw default allow outgoing; ufw allow 22,80,443/tcp; ufw enable`  <br>Fail2ban: `/etc/fail2ban/jail.local`|
|**4. Docker и Docker-Compose**|🔹 Установка контейнеризации|Понимание современных деплой-инструментов|```bash curl -fsSL [https://get.docker.com](https://get.docker.com)|
|**5. Web-сервер (Nginx)**|🔹 Раздача статического сайта / proxy к контейнерам|Понимание веб-сервера, reverse-proxy|`bash apt install -y nginx systemctl enable --now nginx`  <br>Файл `/etc/nginx/sites-available/default` или `/etc/nginx/sites-available/example.conf`|
|**6. HTTPS-сертификаты**|🔹 Let’s Encrypt / Certbot|Безопасное шифрование|`bash apt install -y certbot python3-certbot-nginx certbot --nginx -d example.com -d www.example.com`|
|**7. MySQL / MariaDB**|🔹 Установка, создание БД, пользователи|Работа с SQL, доступ из PHP/Python|`bash apt install -y mariadb-server systemctl enable --now mariadb mysql_secure_installation`  <br>Создать БД: `CREATE DATABASE testdb;`|
|**8. Docker + MySQL**|🔹 Развернуть MySQL в контейнере, подключить volume|Контейнеризация баз данных|`bash docker run -d --name mysql -e MYSQL_ROOT_PASSWORD=1234 -v /srv/mysql:/var/lib/mysql -p 3306:3306 mysql:8`|
|**9. Web + DB (пример LEMP)**|🔹 Nginx + PHP-FPM + MySQL|Понимание веб-стека|`bash apt install -y php-fpm php-mysql echo "<?php phpinfo(); ?>" > /var/www/html/info.php`|
|**10. Docker Compose проект**|🔹 Собрать стек nginx + php + mysql через Compose|Практика DevOps|Создай `docker-compose.yml` с 3 сервисами; настрой `.env` и `volumes`|
|**11. Почтовый сервер (тренировка)**|🔹 Построить Postfix + Dovecot + DKIM (или Mailcow)|Понимание email-систем|Начни с `apt install postfix dovecot-imapd` или `mailcow-dockerized`|
|**12. Мониторинг и логи**|🔹 journalctl, systemctl, logrotate|Навыки анализа и отладки|`bash journalctl -xeu nginx.service`  <br>Настроить `logrotate /etc/logrotate.conf`|
|**13. Backups и снапшоты**|🔹 rsync, tar, mysqldump, cron|Резервное копирование|`bash crontab -e 0 3 * * * /usr/bin/mysqldump -u root -p1234 db > /backup/db.sql`|
|**14. Автоматизация / CI**|🔹 cron, systemd units, Git|Администрирование через скрипты|Примеры `bash`-скриптов, cron-тасков|
|**15. Hardening и оптимизация**|🔹 Fail2ban, SSH-ключи, swap, sysctl|Повышение стабильности|Проверка `/etc/ssh/sshd_config`, `/etc/sysctl.conf`|

---

# 🧩 Дополнительно — структура каталогов

`/home/viktor/        # личные файлы и проекты /var/www/            # веб-сайты (nginx root) /srv/docker/         # docker-compose проекты /opt/mailcow/        # если будешь ставить mailcow /backup/             # резервные копии`

---

# 🚀 Пошаговая практика (рекомендуемый порядок)

1️⃣ **Базовая настройка сервера** — SSH, sudo, firewall.  
2️⃣ **Docker** — протестируй контейнеры (nginx, mysql, phpmyadmin).  
3️⃣ **Docker Compose** — собери простой стек LEMP.  
4️⃣ **Nginx как reverse proxy** — проксируй контейнеры по поддоменам.  
5️⃣ **HTTPS (Let’s Encrypt)** — для `app.example.com`, `db.example.com`, `mail.example.com`.  
6️⃣ **База данных** — попробуй подключение с локального клиента и через контейнер.  
7️⃣ **Mailcow или Postfix** — настрой для тренировки почту.  
8️⃣ **Мониторинг / резервное копирование** — скрипт бэкапа БД и /srv.  
9️⃣ **Автоматизация и Hardening** — cron-таски, Fail2Ban, обновления.

---

# 📚 Что можно изучать параллельно

| Раздел     | Что почитать / потренировать           |
| ---------- | -------------------------------------- |
| Linux      | systemctl, journalctl, grep, awk, sed  |
| Docker     | Dockerfile, Compose, Volumes, Networks |
| Nginx      | reverse proxy, HTTPS, caching          |
| SQL        | CREATE / SELECT / JOIN / INDEX         |
| Mail       | SMTP, DKIM, SPF, DMARC                 |
| Security   | Fail2ban, ufw, SSL-hardening           |
| Automation | cron, bash-скрипты, systemd-таймеры    |
## ШАГ 1
```bash
#успешно выполнены команды 

apt update && apt -y upgrade && apt -y autoremove
apt install -y sudo vim ufw fail2ban htop net-tools curl wget git lsb-release ca-certificates

adduser viktor
usermod -aG sudo viktor

ssh-keygen -t ed25519 -C "viktor@contabo"
mkdir -p /home/viktor/.ssh
nano /home/viktor/.ssh/authorized_keys

chmod 700 /home/viktor/.ssh
chmod 600 /home/viktor/.ssh/authorized_keys
chown -R viktor:viktor /home/viktor/.ssh

sudo nano /etc/ssh/sshd_config

PermitRootLogin no
PasswordAuthentication no

systemctl restart ssh

ssh viktor@185.182.184.40

sudo /usr/sbin/ufw status

sudo /usr/sbin/ufw default deny incoming
sudo /usr/sbin/ufw default allow outgoing
sudo /usr/sbin/ufw allow 22/tcp
sudo /usr/sbin/ufw enable
sudo /usr/sbin/ufw status verbose

echo 'export PATH=$PATH:/usr/sbin' >> ~/.bashrc
source ~/.bashrc


sudo apt install -y fail2ban

sudo systemctl enable fail2ban
sudo systemctl start fail2ban

sudo systemctl status fail2ban

sudo cp /etc/fail2ban/jail.conf /etc/fail2ban/jail.local
sudo nano /etc/fail2ban/jail.local



[sshd]
enabled = true
port    = ssh
filter  = sshd
logpath = /var/log/auth.log
maxretry = 5
bantime = 600
findtime = 600


sudo systemctl restart fail2ban

sudo timedatectl set-timezone Europe/Berlin
sudo timedatectl status

```
классная работа! ниже — обещанная шпаргалка по всем твоим командам: что делает каждая, зачем нужен ключ/параметр и почему именно так.

# Этап 1. Обновления и базовые утилиты

```bash
apt update && apt -y upgrade && apt -y autoremove
```

- `apt update` — обновляет индекс пакетов (список доступных версий).
    
- `apt -y upgrade` — обновляет установленные пакеты до новых версий. `-y` автоматически отвечает «yes», чтобы не ждать подтверждения.
    
- `apt -y autoremove` — удаляет зависимые пакеты, которые больше не нужны (чистит систему).
    

```bash
apt install -y sudo vim ufw fail2ban htop net-tools curl wget git lsb-release ca-certificates
```

- `install` — установка перечисленных пакетов.
    
- `-y` — не спрашивать подтверждение.
    
- смысл пакетов:
    
    - `sudo` — безопасное выполнение команд с привилегиями.
        
    - `vim` — редактирование конфигов.
        
    - `ufw` — простой фаервол.
        
    - `fail2ban` — блокировка брутфорса.
        
    - `htop` — удобный просмотр процессов.
        
    - `net-tools` — классические сетевые утилиты (`ifconfig`, `netstat`).
        
    - `curl`/`wget` — скачивание по http/https.
        
    - `git` — управление репозиториями.
        
    - `lsb-release` — сведения о дистрибутиве, полезно скриптам.
        
    - `ca-certificates` — корневые сертификаты для TLS (https).
        

# Этап 1. Пользователь и привилегии

```bash
adduser viktor
```

- создаёт пользователя `viktor`, домашний каталог `/home/viktor`, задаёт пароль и базовые поля.
    

```bash
usermod -aG sudo viktor
```

- добавляет (`-a`, append) пользователя в группу (`-G`) `sudo`.
    
- `-a` обязателен, чтобы **добавить**, а не **заменить** список групп.
    

# Этап 1. Ключи SSH и доступ

```bash
ssh-keygen -t ed25519 -C "viktor@contabo"
```

- генерирует пару ключей SSH.
    
- `-t ed25519` — современный быстрый и безопасный алгоритм (обычно предпочтительнее RSA).
    
- `-C` — комментарий (метка), чтобы отличать ключи.
    

```bash
mkdir -p /home/viktor/.ssh
```

- `-p` — создаёт все недостающие каталоги (и не ругается, если уже есть).
    

```bash
nano /home/viktor/.ssh/authorized_keys
```

- открываешь файл, куда вставляешь **публичный** ключ (строка из `id_ed25519.pub`).
    

```bash
chmod 700 /home/viktor/.ssh
chmod 600 /home/viktor/.ssh/authorized_keys
```

- права по безопасности SSH:
    
    - `700` для каталога — только владелец может читать/писать/выполнять.
        
    - `600` для файла ключей — только владелец может читать/писать.
        
    - слишком «широкие» права ломают авторизацию по ключу.
        

```bash
chown -R viktor:viktor /home/viktor/.ssh
```

- выставляет владельца каталога и содержимого (`-R`) пользователю `viktor` и группе `viktor`.
    
- важно, иначе SSH может игнорировать ключи.
    

# Этап 1. Усиление SSH

```bash
sudo nano /etc/ssh/sshd_config
```

- редактируем конфигурацию SSH-сервера.
    

```
PermitRootLogin no
PasswordAuthentication no
```

- `PermitRootLogin no` — запрет прямого входа root (минимизация риска).
    
- `PasswordAuthentication no` — полностью отключает вход по паролю; остаются только ключи (намного безопаснее).
    

```bash
systemctl restart ssh
```

- перезапускает службу `ssh`, чтобы применить изменения.
    

```bash
ssh viktor@185.182.184.40
```

- проверка входа по ключу под новым пользователем.
    

# Этап 1. Фаервол (UFW)

```bash
sudo /usr/sbin/ufw status
```

- показывает статус UFW («inactive»/«active»). Полный путь пригодился, потому что у неприоритетных пользователей иногда `/usr/sbin` нет в `$PATH`.
    

```bash
sudo /usr/sbin/ufw default deny incoming
sudo /usr/sbin/ufw default allow outgoing
```

- политики по умолчанию:
    
    - `deny incoming` — всё входящее запрещено (принцип «запрещено всё, кроме разрешённого»).
        
    - `allow outgoing` — исходящий трафик разрешён (обычно удобно на VPS).
        

```bash
sudo /usr/sbin/ufw allow 22/tcp
```

- разрешаем SSH на порту 22 по TCP, иначе после включения фаервола потеряешь доступ.
    

```bash
sudo /usr/sbin/ufw enable
```

- активирует UFW и применяет текущие правила (с предупреждением о возможном разрыве ssh — мы уже открыли 22, всё ок).
    

```bash
sudo /usr/sbin/ufw status verbose
```

- подробный статус (показывает политики по умолчанию, логирование и все правила).
    

```bash
echo 'export PATH=$PATH:/usr/sbin' >> ~/.bashrc
source ~/.bashrc
```

- добавляем `/usr/sbin` в PATH текущего пользователя навсегда; `source` применяет изменение без выхода из оболочки.
    

# Этап 1. Fail2Ban (защита от брутфорса)

```bash
sudo apt install -y fail2ban
```

- установка службы; `-y` — без подтверждения.
    

```bash
sudo systemctl enable fail2ban
```

- добавляет Fail2Ban в автозапуск (создаёт symlink в systemd). Делается один раз.
    

```bash
sudo systemctl start fail2ban
```

- запуск сервиса в текущей сессии.
    

```bash
sudo systemctl status fail2ban
```

- выводит состояние; ищем `active (running)`.
    

```bash
sudo cp /etc/fail2ban/jail.conf /etc/fail2ban/jail.local
```

- Fail2Ban рекомендует **не править** `jail.conf`.
    
- `jail.local` — локальные переопределения, которые не затрутся при обновлении.
    

```bash
sudo nano /etc/fail2ban/jail.local
```

- открываем файл локальной конфигурации.
    

```
[sshd]
enabled = true      # включаем фильтр для SSH
port    = ssh       # имя сервиса из /etc/services (обычно 22)
filter  = sshd      # шаблон разбора логов для SSH
logpath = /var/log/auth.log  # где Fail2Ban ищет неудачные попытки
maxretry = 5        # сколько неудачных попыток до бана
bantime = 600       # время бана (сек) — здесь 10 минут
findtime = 600      # окно, в которое считаются попытки (сек)
```

- эти параметры балансируют между безопасностью и удобством.
    
- `maxretry=5` — не слишком строгий, но уже отсекает брутфорс; `bantime` можно увеличивать при реальных атаках.
    

```bash
sudo systemctl restart fail2ban
```

- перечитывает конфиг и перезапускает сервис.
    

# Этап 1. Время и часовой пояс

```bash
sudo timedatectl set-timezone Europe/Berlin
```

- устанавливает часовой пояс; важно для логов, cron и сертификатов.
    

```bash
sudo timedatectl status
```

- показывает текущее состояние времени/таймзоны и синхронизации NTP.
    

---

## Частые проверки/диагностика

- Проверить доступность SSH:
    
    ```bash
    sudo ufw status
    systemctl status ssh
    journalctl -xeu ssh
    ```
    
- Проверить, что Fail2Ban реально работает по jail `sshd`:
    
    ```bash
    sudo fail2ban-client status
    sudo fail2ban-client status sshd
    ```
    
- Проверить логи авторизации:
    
    ```bash
    sudo tail -f /var/log/auth.log
    ```
    

---

## Почему такой порядок важен

1. **Ключи SSH** настраиваются **до** отключения паролей — чтобы не потерять доступ.
    
2. **UFW** включается **после** открытия порта 22 — по той же причине.
    
3. **Fail2Ban** — надстройка поверх логов и фаервола, поэтому ставится после базового SSH и UFW.
    
4. **Часовой пояс** — чтобы потом в логах и cron не путаться со временем.
    

---

%% Если хочешь, продолжу шпаргалку следующими этапами:

- **Этап 2:** hostname + DNS + rDNS (подготовка к почте/SSL),
    
- **Этап 3:** Docker + Compose (установка, группы, тестовый `nginx`),
    
- **Этап 4:** Nginx + HTTPS (Let’s Encrypt),
    
- **Этап 5:** MySQL/MariaDB (в системе и в Docker). %%

Отлично, Виктор 👏  
Ты сделал всё **идеально правильно** — сервер теперь имеет корректное доменное имя и DNS-запись!

Разберём подробно, что ты сделал и зачем, чтобы закрепить это как **Этап 2: Сеть и DNS** в твоей шпаргалке.

---

# 🧩 Этап 2. Сеть и DNS

| Цель | Настроить уникальное имя сервера (hostname/FQDN), проверить IP, DNS и связку прямой ↔ обратной записи. |

---

## 🔹 1. Установка имени сервера (hostname)

```bash
sudo hostnamectl set-hostname s1.viktorplus.com
```

- **`hostnamectl`** — системная утилита systemd для управления именем хоста.
    
- `set-hostname` задаёт постоянное имя, сохраняемое в `/etc/hostname`.
    
- Указано **FQDN** (full qualified domain name) — `s1.viktorplus.com`, что важно для SSL и почтовых серверов.
    

Проверка:

```bash
hostnamectl
```

✅ Результат показывает:

```
Static hostname: s1.viktorplus.com
Operating System: Debian GNU/Linux 13 (trixie)
Architecture: x86-64
```

---

## 🔹 2. Проверка сетевых интерфейсов

```bash
ip a
```

- **`ip a`** (или `ip addr`) выводит все сетевые интерфейсы и их IP.
    
- В твоём случае:
    
    - интерфейс `eth0`;
        
    - IPv4: `185.182.184.40`;
        
    - IPv6: присутствует, но пока не нужен;
        
    - состояние `state UP` — интерфейс активен.
        

💡 Поле `brd 185.182.187.255` — широковещательный адрес подсети Contabo.

---

## 🔹 3. Настройка /etc/hosts

```bash
sudo nano /etc/hosts
```

Добавь строку:

```
185.182.184.40   s1.viktorplus.com s1
```

📘 `/etc/hosts` обеспечивает локальное разрешение имени, даже если DNS временно недоступен.  
Первая часть — IP, затем полное имя (FQDN), потом краткое.

---

## 🔹 4. Создание A-записи в панели домена

Ты добавил запись:

```
Type: A
Name: s1
Value (Destination IP): 185.182.184.40
```

Это связывает поддомен **s1.viktorplus.com** с IP сервера.  
A-запись теперь активна (см. «Active» в панели).

---

## 🔹 5. Проверка DNS-резолвинга

```bash
dig s1.viktorplus.com +short
```

или короче:

```bash
host s1.viktorplus.com
```

✅ Результат:

```
185.182.184.40
```

Значит DNS работает и имя резолвится в IP.

---

## 🔹 6. (Опционально) Обратная запись (PTR/rDNS)

В панели **Contabo → Reverse DNS** задай:

```
PTR: s1.viktorplus.com
```

📘 PTR должен указывать обратно на A-запись — это критично для почтовых серверов и репутации IP.

Проверить потом можно:

```bash
sudo apt update
sudo apt install -y dnsutils

dig s1.viktorplus.com +short
dig -x 185.182.184.40 +short
```

Ожидаемый ответ: `s1.viktorplus.com.`

```
|Команда|Назначение||---|---|
|`dig example.com`|Полная DNS-информация: серверы, TTL, тип записи|
|`dig +short example.com`|Только короткий ответ (IP или имя)|
|`dig -x 1.2.3.4 +short`|Обратная зона (PTR — IP → имя)|
|`dig NS domain.com`|Серверы имён (Name Servers)|
|`dig MX domain.com`|Почтовые записи (Mail Exchange)|
```

---

## 🔹 7. Зачем это всё нужно

|Настройка|Зачем нужна|
|---|---|
|**hostname**|внутреннее имя системы, используется в логах и для TLS|
|**A-record**|чтобы доменное имя указывало на IP|
|**/etc/hosts**|резервное локальное разрешение (не зависит от DNS)|
|**PTR/rDNS**|подтверждает, что IP принадлежит домену; критично для SMTP|
|**Проверка dig/host**|убедиться, что DNS реально работает|

---

## ✅ Результат этапа 2

|Компонент|Статус|
|---|---|
|Hostname|`s1.viktorplus.com` ✅|
|A-запись DNS|указывает на 185.182.184.40 ✅|
|/etc/hosts|содержит FQDN ✅|
|Проверка `dig`|возвращает IP ✅|

---

